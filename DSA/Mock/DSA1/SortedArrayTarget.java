package DSA.Mock.DSA1;

//Given a rotated array and target, return the index of target in array and time complexity Should be O(log(n))

public class SortedArrayTarget {
  /* Java program to search an element
    in a sorted and pivoted array*/
  /*Serches an element key in a
    pivoted sorted array arrp[] of size n*/

  //    static int pivotedBinnarySearch(int arr[],int size, int key){
  //        int pivot=findPivot(arr,0,size-1);
  //
  //        //If we didn't find a pivot, then
  //        //aray is not rotated at all
  //        if (pivot==-1)
  //            return binarySearch(arr,0,size-1,key);
  //
  //        /*if we found a pivot, than first
  //        compare with pivot and then
  //        search in two subarrays around pivot
  //         */
  //        if (arr[pivot]==key)
  //            return pivot;
  //        if (arr[0]<=key)
  //            return binarySearch(arr,0,pivot-1,key);
  //        return binarySearch(arr,pivot+1,size-1,key);
  //    }
  //    /*
  //    Function to get
  //     */
  //    static int findPivot(int arr[],int low, int high){
  //        if (high<low)
  //            return -1;
  //            if (high==low)
  //                return low;
  //
  //    }
  //    static int binarySearch(int arr[], int low,int high, int key) {
  //
  //    }
}
